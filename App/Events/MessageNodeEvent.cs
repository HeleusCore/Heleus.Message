namespace Heleus.Apps.Message
{
    public class MessageNodeEvent
    {
        public readonly MessageNode Node;

        public MessageNodeEvent(MessageNode node)
        {
            Node = node;
        }
    }
}
